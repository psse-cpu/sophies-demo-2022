// strategy taken from here - actually very good

import routes from './routes'

describe('router', () => {
  describe('beforeGuard', () => {
    it('should fail', () => {
      expect(routes).toBeTruthy()
    })
  })
})
